1.Change database connection setings in the file \eBanking\WEB-INF\classes\db.properites

2.bulid a database named ebank in mysql and run the code in ebank.sql

3.deploy the system to tomcat

4.run tomcat, and then the system can be used

5.account and user information can be get in database. Using test username "a" and password "a" can login 
  the system. 

access: localhost:port/eBanking/


All the pages in \eBanking\WebRoot\WEB-INF\pages

Background code in \eBanking\src
